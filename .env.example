# Public app origin (local, Vercel preview, or production).
VITE_APP_URL=

# Supabase browser client. Use the publishable (anon) key only.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Server/script only — never prefix with VITE_ and never put this in Vercel client env.
TMDB_API_READ_TOKEN=
